query SearchNuzlockes($input: SearchInput) {
  searchNuzlockes(input: $input) {
    results {
      id
      title
      type
      encounters {
        id
        nickname
        pokemon {
          id
          types
          sprite
        }
      }
    }
  }
}
